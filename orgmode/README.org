#+title: Site source

Handbook is =orgmode/handbook/*.org=.

#+begin_src console
$ ./org/build.sh
#+end_src

That is =org-publish= + =ox-html=. Batch faces come from engrave-faces
(the vault exporter), because htmlize needs a display. Tracker crate
docs: https://vissue.rgoswami.me
